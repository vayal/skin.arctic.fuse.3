# Velocity (redirect)

Fork-specific **definitions, rules, inventories, journeys, D-038, testing guide, and deep fork docs** now live in a single tree:

**→ [`../context/README.md`](../context/README.md)**

Older links to `doc/velocity/*.md` should be updated to the matching file under **`doc/context/`** (same filenames for moved documents, plus [`journeys/`](../context/journeys/README.md) under `doc/context/journeys/`).
